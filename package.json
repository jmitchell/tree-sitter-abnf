{
  "name": "tree-sitter-abnf",
  "version": "0.0.1",
  "description": "ABNF grammar for tree-sitter",
  "main": "index.js",
  "scripts": {
    "test": "./script/generate_examples.sh && tree-sitter parse examples --quiet --time && tree-sitter test",
    "generate": "tree-sitter generate && npm install"
  },
  "keywords": [
    "parser",
    "lexer"
  ],
  "author": "Jacob Mitchell",
  "license": "MIT",
  "dependencies": {
    "nan": "^2.11.1"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.13.10"
  }
}
